## План рефакторинга перед следующим этапом

Цель: убрать техдолг, который будет мешать дальше развивать CalDAV/календарный поток, и снизить риск регрессий.

---

### Архитектура по слоям (чтобы развивать “в удовольствие”)

Принцип: **каждый слой зависит только от слоёв ниже**. UI не знает о провайдерах и не делает “сетевых” действий напрямую. Кэш/индексация — инфраструктурный слой, который можно включать/выключать без изменения логики отображения.

#### 1) Слои и ответственность

- **UI слой (Presentation)**
  - **Что**: Views/Settings/DOM рендеринг.
  - **Где**: `src/views/*`, `src/ui/*`, `styles.css`
  - **Зависит от**: только от _Application layer_ (сервисы/фасады) и DTO/типов.
  - **Не должен**: обращаться к `requestUrl`, `DAVClient`, vault напрямую (кроме отображения/команд, если мы сознательно оставляем).

- **Application layer (Use-cases / Orchestration)**
  - **Что**: сценарии “refresh”, “sync to notes”, “schedule notifications”, “authorize OAuth”.
  - **Где**: `src/sync/syncService.ts`, части `main.ts` (фасад/команды), будущие `src/app/*`
  - **Зависит от**: Domain + Repositories + Gateways.
  - **Отвечает за**: порядок шагов, ретраи, запись логов, метрики “stale”.

- **Domain layer (модель и бизнес-правила)**
  - **Что**: чистые функции/правила над сущностями: фильтры горизонта, уникализация, выбор “earliest per key”, разбор статусов, нормализация.
  - **Где**: `src/calendar/ics.ts` (частично), будущие `src/domain/*`
  - **Зависит от**: ничего (или только от типов/утилит).
  - **Важно**: domain не должен знать про Obsidian API / сеть / файловую систему.

- **Infrastructure: Providers/Gateways**
  - **Что**: получение данных из внешнего мира (CalDAV/ICS), OS уведомления, Obsidian requestUrl fetch.
  - **Где**: `src/calendar/providers/*`, `src/caldav/*`, `src/os/*`
  - **Зависит от**: Obsidian API/tsdav/Node окружения.

- **Infrastructure: Storage/Repositories (кэш, индексы, хранение)**
  - **Что**: хранение “текущего состояния” событий, индексы для быстрых выборок, кэш “последних успешных данных”.
  - **Где (предложение)**: `src/calendar/store/*` или `src/storage/*`
  - **Зависит от**: (опционально) Obsidian `loadData/saveData` для persistent cache.

#### 2) Контракты между слоями (главная “прозрачность”)

**Вместо** передачи “сырого массива событий” везде, вводим единый контракт результата обновления:

- `RefreshResult` (DTO):
  - `events: CalendarEvent[]`
  - `perCalendar: Record<calendarId, { status: "fresh" | "stale"; fetchedAt?: number; error?: string }>`
  - `updatedAt: number`

UI получает **только** `RefreshResult` (или store-срезы), и рендерит:

- события на день/диапазон,
- бейдж “stale” если часть календарей не обновилась,
- не решает _как_ обновлять и _где_ брать кэш.

#### 3) Где живёт кэш (чтобы не трогать логику и оформление)

Рекомендованный минимальный слой:

- `CalendarEventStore` (in-memory) держит:
  - `currentEvents`
  - `lastGoodByCalendarId`
  - `perCalendarStatus` (fresh/stale + error + fetchedAt)
  - индексы: `byCalendarId`, `byDay`, `byUid/eventKey`

Persistent слой (опционально):

- `CalendarEventCache` (disk) хранит “последний успешный снапшот” (без секретов), чтобы после перезапуска UI не был пустым.

#### 3.1) Obsidian-native “база/индекс” на md (frontmatter) — как слой хранения и индексирования

Идея: использовать **родной механизм Obsidian** — `metadataCache`/frontmatter/поиск — как “базу данных”, где:

- плагин пишет сущности в `.md` (уже делаем для встреч/протоколов),
- Obsidian автоматически индексирует метаданные,
- человек может искать/сортировать/строить таблицы (особенно удобно с Dataview, но не обязательно).

Почему это хорошо:

- **Нативно**: не нужно изобретать БД — Obsidian уже строит индекс по файлам.
- **Прозрачно**: кэш/индекс — это **файлы**, их видно, можно grep/поиск/ссылки.
- **Совместимо**: Dataview (если установлен) даст “таблицы/фильтры” без доп.кода.

Как это увязать со слоями:

- Это **Infrastructure: Storage/Repositories** (vault-backed).
- UI/Domain не знают, где хранится индекс (в памяти/на диске/в md) — они работают через Store/Repository.

Предложение по структуре сущностей (минимум):

- Встреча: `assistant_type: calendar_event` (уже есть) + ключевые поля в frontmatter (`event_key`, `calendar_id`, `uid`, `start`, `end`, `summary`, `status`, `my_partstat`…)
- Протокол: `assistant_type: protocol` (уже есть) + `event_key`, `start`, `end`, ссылки.
- Люди: `assistant_type: person` (план) + `email`, `display_name`, `aliases`, связи на встречи/протоколы.
- Проекты: `assistant_type: project` (план) + `slug`, `title`, связи на встречи/протоколы/людей.

Индекс/дашборды для человека (опционально, но очень полезно):

- Папка (в настройках): например `Ассистент/Индекс/`
- Файлы:
  - `Ассистент/Индекс/Встречи.md` — список/таблица встреч (по дате, проекту, людям)
  - `Ассистент/Индекс/Протоколы.md`
  - `Ассистент/Индекс/Люди.md`
  - `Ассистент/Индекс/Проекты.md`
- Содержимое:
  - без Dataview: простые ссылки/заголовки/поиск по `assistant_type: ...`
  - с Dataview: таблицы `TABLE start, summary, project FROM "Ассистент/Встречи" WHERE assistant_type="calendar_event" SORT start desc`

Важно (ограничения/нюансы):

- `metadataCache` не гарантирует мгновенную актуальность при массовых правках → для логики плагина всё равно нужен **свой Store/Repository** (in-memory), а Obsidian-index — это “источник истины на диске” + UX для человека.
- Чтобы не ломать пользовательские заметки, любые авто-обновления нужно делать через **машинные секции/маркеры** (как уже сделано для протоколов/заметок встречи).

Внедрение кэша тогда выглядит так:

- Providers возвращают `CalendarEvent[]` или ошибку.
- Application layer собирает `RefreshResult`.
- Store принимает `RefreshResult` и обновляет индексы/кэш.
- UI читает Store (через `CalendarService`/фасад) и рисует.

#### 4) Правила зависимостей (жёстко)

- `src/views/*` и `src/ui/*` **не импортируют** `src/calendar/providers/*`, `src/caldav/*`, `obsidianFetch`, `tsdav`.
- Providers **не знают** про UI и не используют `Notice`.
- Логирование (`LogService`) — доступно из Application layer; Domain не логирует.

---

### P0 — критично (риски/несостыковки, которые могут “кусать”)

- ✅ **Синхронизировать правило про App password (пробелы)**
  - **Проблема**: в `src/calendar/providers/caldavProvider.ts` для Google Basic сейчас пароль **автоматически чистится** (`replaceAll(" ", "")`), хотя в UX/описаниях мы договорились “не править автоматически, а объяснять”.
  - **Зачем**: иначе будет неочевидное поведение (пользователь вводит одно, реально уходит другое), плюс документация/UI начнут противоречить фактическому коду.
  - **Где**: `src/calendar/providers/caldavProvider.ts` (формирование `credentials.password` для Basic).
  - **Что сделать (варианты)**:
    - **A (предпочтительно для UX)**: убрать автозамену, оставить только подсказку “вводить без пробелов”.
    - **B**: оставить автозамену, но явно закрепить это в UI/README (“пробелы будут удалены автоматически”) и в логике валидатора.
  - **Статус**: выбран вариант A — автозамены нет, в UI есть подсказка.

- ✅ **Ускорить `syncEvents`: убрать O(N файлов) на каждую встречу**
  - **Проблема**: `EventNoteService.findEventFileByStableId()` перебирает `vault.getFiles()` при каждом `resolveEventFilePath()`; на большом vault и/или большом числе событий это станет заметно.
  - **Зачем**: производительность и предсказуемость фонового автообновления.
  - **Где**: `src/calendar/eventNoteService.ts` (`resolveEventFilePath`, `findEventFileByStableId`).
  - **Что сделать**:
    - во время `syncEvents()` построить индекс `sid -> TFile` одним проходом по файлам **только в папке встреч**;
    - использовать индекс в `resolveEventFilePath()` вместо полного `vault.getFiles()`.
  - **Статус**: реализовано (индекс строится один раз на `syncEvents()`, поиск по `sid` стал O(1) по map).

- ✅ **Убрать дублирование сетевого транспорта Obsidian requestUrl**
  - **Проблема**: одинаковая логика конвертации headers/body и сборки `Response` продублирована в `src/caldav/obsidianFetch.ts` и `src/caldav/crossFetchObsidian.ts`.
  - **Зачем**: чтобы багфиксы/улучшения (например корректная обработка бинарного body) не расходились в двух реализациях.
  - **Что сделать**: вынести общий код в один модуль (например `src/caldav/requestUrlFetch.ts`) и переиспользовать в обоих местах.
  - **Статус**: реализовано (`src/caldav/requestUrlFetch.ts`, оба consumers переведены на него).

---

### P1 — важно (поддерживаемость и скорость разработки)

- ✅ **Obsidian-native индекс по сущностям (md + frontmatter)**
  - **Идея**: использовать “Ассистент/\*” как видимую базу: встречи/протоколы/люди/проекты — отдельные md файлы с нормализованным frontmatter.
  - **Зачем**:
    - плагину проще строить индекс/связи через `metadataCache`,
    - человеку доступны поиск/сортировка/Dataview отчёты.
  - **Что сделать (MVP)**:
    - стабилизировать frontmatter-схему для `calendar_event` и `protocol` (уже почти сделано),
    - добавить (настройкой) папку “Индекс” и шаблонные index notes (пустые либо с Dataview-блоками) — ✅ сделано (создаём 4 файла: Встречи/Протоколы/Люди/Проекты, не перезаписываем существующие),
    - для People/Projects — начать создавать карточки (пока без “умного” извлечения, но с ручным заполнением).

- ✅ **Кэш событий календаря + “stale режим” (offline-first UX)**
  - **Запрос/идея**: если сеть недоступна или провайдер упал, пользователь всё равно видит **последние успешно полученные события**, а в логах/интерфейсе явно видно, что данные **устарели** и обновление не удалось.
  - **Зачем**:
    - “повестка” остаётся полезной без сети (особенно на ноуте/в поездке);
    - снижаем фрустрацию “всё пустое” при временных сбоях;
    - даём прозрачную диагностику в логах.
  - **Где**: `src/calendar/calendarService.ts`, `src/sync/syncService.ts`, `src/views/agendaView.ts`, `src/log/logService.ts`.
  - **Что сделать (минимум / MVP)**:
    - хранить в памяти `lastGoodByCalendarId: Map<calendarId, { events, fetchedAt }>` и `lastErrorByCalendarId`.
    - при `refreshAll()`:
      - если календарь успешно обновился → обновить `lastGood...`;
      - если календарь упал → **оставить** его lastGood‑события в общей выдаче, но отметить статусом “stale”.
    - в логах писать: `Календарь: обновление не удалось, использую кэш` + причина.
    - в UI (Повестка) показывать небольшой баннер/бейдж, если есть stale‑календари (например “⚠️ Данные устарели: 1/3 календарей, см. лог”).
  - **Статус**: ✅ сделано (in-memory store + логирование “использую кэш” + баннер + persistent cache на диск в системной папке плагина, без `url`).
  - **Дальше (опционально)**:
    - ограничить размер файла: обрезать до N событий/календарь (плюс учитывать горизонт 60 дней).

- 🚧 **Обёртка/репозиторий для событий с индексацией**
  - **Идея**: поверх массива `CalendarEvent[]` сделать слой, который держит индексы и ускоряет выборки, чтобы дальше добавлять фичи (поиск, фильтры, “сегодня/неделя”, группировки, статусы) без постоянных O(N) проходов.
  - **Зачем**: производительность + удобство развития (особенно когда появятся People/Projects, связи, расширенные фильтры).
  - **Где**: `src/calendar/` (новый модуль), используется из `CalendarService`, `AgendaView`, `NotificationScheduler`, `EventNoteService`.
  - **Что сделать**:
    - новый класс/модуль, например `EventStore`/`CalendarEventRepository`:
      - `setEvents(events, { fetchedAt, staleCalendars, errors })`
      - индексы: `byCalendarId`, `byDay(YYYY-MM-DD)`, `byUid` (и/или `byEventKey`), возможно `byStartTs` (sorted массив).
      - методы выборок: `getDay(offset)`, `getRange(start,end)`, `getUpcoming(horizonMs)`, `getByUid(calendarId, uid)`.
    - хранить метаданные свежести рядом с событиями (`sourceStatus`), чтобы UI/логика могли показывать “stale” без костылей.
  - **Статус (MVP)**: ✅ введён in-memory `CalendarEventStore` + `perCalendarStatus`, включено логирование stale (“использую кэш”) и баннер в повестке.

- **Разбить `src/ui/settingsTab.ts` на секции**
  - **Проблема**: файл ~1k строк, смешаны UI-рендер, доменная логика readiness, тексты подсказок, кнопки и side effects.
  - **Зачем**: чтобы изменения в CalDAV/календарях не превращались в “страшно трогать”, и чтобы проще было тестировать/читать.
  - **Что сделать**:
    - вынести секции в отдельные функции/модули (например `renderNotificationsSection`, `renderCaldavAccountsSection`, `renderConnectedCalendarsSection`, `renderCalendarOperationsSection`);
    - вынести повторяющиеся элементы (notice blocks, password fields + “глазик”, маленькие action-кнопки) в небольшие UI-хелперы.
  - **Статус**: ✅ сделано (display() теперь делегирует в `render*Section`, код разбит на методы без изменения поведения).

- **Единый валидатор/`readiness` CalDAV аккаунта**
  - **Проблема**: проверки “готов/не готов” сейчас частично в UI, частично в provider (guard-и).
  - **Зачем**: единое поведение, меньше расхождений, проще расширять (добавить новые причины/политики).
  - **Где**: `src/ui/settingsTab.ts`, `src/calendar/providers/caldavProvider.ts`.
  - **Что сделать**:
    - вынести функцию `getCaldavAccountReadiness(account)` в `src/caldav/`;
    - UI использует её для отображения причин;
    - provider использует её для раннего отказа с понятной ошибкой.
  - **Статус**: ✅ сделано (`src/caldav/caldavReadiness.ts`, импорт в UI и provider).

- **Общие утилиты для md-шаблонов и секций**
  - **Проблема**: дублируются `yamlEscape`, “маркерные секции”, `ensureFile` и похожие куски.
  - **Зачем**: меньше копипасты, проще менять шаблоны без регрессий.
  - **Где**: `src/calendar/eventNoteService.ts`, `src/protocols/protocolNoteService.ts`, `src/log/logFileWriter.ts`.
  - **Что сделать**:
    - `src/vault/ensureFile.ts` (единый ensure/create для markdown файлов);
    - `src/vault/markdownSections.ts` (yamlEscape, mergePreservingSections/markers).
  - **Статус**: ✅ частично сделано (`src/vault/ensureFile.ts`, `src/vault/yamlEscape.ts` + переиспользование). `markdownSections.ts` (маркеры/merge) — ещё можно вынести позже.

- **Единые константы горизонтов/лимитов**
  - **Проблема**: “60 дней” и “48 часов” зашиты в нескольких местах (CalDAV, ICS, Sync, Scheduler).
  - **Зачем**: проще менять поведение и не ловить “несогласованные горизонты”.
  - **Где**: `src/calendar/providers/caldavProvider.ts`, `src/calendar/providers/icsUrlProvider.ts`, `src/sync/syncService.ts`, `src/notifications/notificationScheduler.ts`.
  - **Что сделать**: собрать в `src/calendar/constants.ts` и использовать везде.
  - **Статус**: ✅ сделано (`src/calendar/constants.ts`, все usage переведены).

---

### P2 — хорошо бы (инженерка/чистота)

- **Добавить `typecheck`-скрипт**
  - **Зачем**: быстрый CI-style прогон типов без сборки.
  - **Где**: `package.json`
  - **Что сделать**: `"typecheck": "tsc -p tsconfig.json --noEmit"`.
  - **Статус**: ✅ сделано (`npm run typecheck`).

- **Разобраться с пустыми директориями**
  - **Проблема**: `src/meetings/` пустая.
  - **Зачем**: меньше шума в дереве проекта, понятнее что живое/что план.
  - **Что сделать**: либо удалить, либо добавить короткий `README.md`/`index.ts` с назначением.
  - **Статус**: ✅ сделано (добавлен `src/meetings/README.md`).

- **Проверить, что логи не утекут секретами**
  - **Зачем**: приватные ICS URL / токены / OAuth данные не должны случайно попадать в лог/markdown.
  - **Где смотреть**: `main.ts` (обработчики ошибок), провайдеры календарей, UI notices.
  - **Что сделать**: при логировании URL — редактировать/маскировать query/token части.
  - **Статус**: ✅ сделано (утилита маскирования `src/log/redact.ts` + тест `tests/redact.test.ts`; URL в `obsidianFetch` маскируется).

---

### Предлагаемый порядок выполнения (минимальный “пакет” на 1–2 вечера)

1. **P0: индекс `sid` в `EventNoteService`** (самый ощутимый перфоманс-выигрыш)
2. **P0: дедуп requestUrl fetch** (снижает риск регрессий CalDAV)
3. **P1: ввести слой Store/Cache + контракт `RefreshResult`** (прозрачная инфраструктура без влияния на UI/Domain)
4. **P1: кэш событий + stale режим на базе Store** (offline-first UX + прозрачные логи)
5. **P1: вынести readiness CalDAV** (единая логика/ошибки/UX)
6. **P1: разнести `settingsTab.ts` по секциям** (скорость будущих изменений)
7. **P2: `typecheck`** (дешёвая инженерная страховка)
