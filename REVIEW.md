# Ревью Assistant (Obsidian plugin)

## Общая оценка: ⭐⭐⭐⭐☆ (4/5)

Проект в хорошем состоянии для MVP‑этапа: есть рабочий календарный поток (ICS/CalDAV → повестка → уведомления → карточки встреч/протоколов), логирование (панель + файловые `.log`) и развитые юнит‑тесты. В сравнении с прошлым ревью заметно выросла устойчивость и UX: появились offline-first (stale режим), `.base` файлы для встроенных Obsidian “Баз”, отображение участников (ATTENDEE/PARTSTAT), “план участия” (приду/не приду/возможно) и более богатые команды.

---

## 📊 Статистика проекта (актуально)

- **Язык/стек**: TypeScript, Obsidian plugin, esbuild, Vitest
- Исходный код**: 41 TS файл в `src/` (~5984 строк)
- Тесты**: 24 TS файла в `tests/`
- Покрытие тестами**: 130+ тестов (Vitest)
- Комментарии (TSDoc/JSDoc)**: ~187 блоков `/** ... */` в `src/`
- **Статус тестов**: ✅ `npm test` — все тесты проходят (Vitest)
- Coverage**: ✅ `npm run test:coverage` (v8): ~92% statements/lines, ~70% branches (ветвления — основная зона роста)
- Скрипты качества**: ✅ `npm run typecheck` (tsc --noEmit)
- Форматирование**: ✅ Prettier (`npm run format`, `npm run format:check`)
- **Зависимости**: `tsdav` (CalDAV), `obsidian` typings, `esbuild`, `vitest`, `jsdom`, `typescript`
- **composer.json**: отсутствует (не PHP‑проект)
- **Версия**: `0.0.1` (из `package.json`)

---

## ✅ Сильные стороны

### 1. Архитектура и структура

- **Хорошая модульность**: разнесено по `src/calendar`, `src/notifications`, `src/views`, `src/log`, `src/vault`, `src/ui`.
- **Разделение Sync vs UI**: `SyncService` оркестрирует refresh/sync/schedule; UI остаётся тонким.
- **Чёткий data‑flow**: `CalendarService` → `AgendaView` + `NotificationScheduler` + синхронизация в заметки встреч.
- **Инфраструктурные базовые кирпичи**: `ensureFolder`, безопасное именование файлов (`createUniqueMarkdownFile`), лог‑сервис.
- План техдолга**: фиксируется в `TZ.md` (чеклист) и в issue‑стиле пунктов внутри документа (без отдельного `REFACTORING.md` в текущем репо).

### 2. Качество кода

- **Strict TS** (`tsconfig.json`), аккуратные типы настроек/сущностей.
- **Деградация по версиям Obsidian**: кастомные SVG‑иконки вместо “ломких” id.
- **Защита vault от мусора**: встречи в прошлом не создаются при refresh (on‑demand по клику).
- **Стабильные идентификаторы встреч**: идентичность держим через frontmatter `(calendar_id, event_id)`; стабильный ключ для индексов = `calendar_id:event_id`. Имя файла человекочитаемое и может безопасно переименовываться.
- Добавлен typecheck**: выделенный `npm run typecheck` сразу ловит регрессии по типам.
- Нормализация настроек**: `normalizeSettings()` заполняет defaults и аккуратно нормализует значения (например `myEmail`).
- **Минимум “технического шума”**: нет `TODO/FIXME` в `src/` (по grep).

### 3. Покрытие тестами

- **Vitest подключен** и реально запускается в CI‑стиле (`npm test`).
- Есть тесты на **ICS парсер**, **планировщик уведомлений**, **vault утилиты (frontmatter)** и **offline-first store/cache**.

### 4. Реализованный функционал

- **MVP‑1 календаря**: ICS URL, повестка (дневная сетка), уведомления (за N минут + в момент начала), автообновление.
- **Логирование**: панель “Лог” + файлы по датам в `<vault>/.obsidian/plugins/assistant/logs/*.log` (не засоряет дерево заметок).
- **Встречи в vault**: создаются md‑карточки встреч, клик в повестке открывает файл.
- **Повторяющиеся встречи (MVP)**: поддержка развёртки `RRULE` (DAILY/WEEKLY) в горизонте (по умолчанию 60 дней), учёт `COUNT/UNTIL/EXDATE`.
- **Протоколы**:
  - “Создать/Открыть протокол” из встречи (контекстное меню стало “умнее”: текущий/последний/создать новый),
  - во встрече хранится список протоколов (машинная секция),
  - в протоколе есть ссылка на встречу.
- **Уведомления**:
  - единый способ доставки: popup‑окно **electron_window** (кнопки: “Диктофон”, “Встреча отменена”, “Закрыть”),
  - safety: fallback на `Notice` если `BrowserWindow` недоступен (тесты/нестандартный runtime).
- **Статус приглашений в повестке (MVP)**: парсинг `ATTENDEE;PARTSTAT=...` и отображение “Принято/Отклонено/Возможно/Нет ответа” (через настройку “мой email”, либо email CalDAV аккаунта по умолчанию).
- Участники встречи**: в карточке встречи сохраняется список `attendees` (email + CN + PARTSTAT) и агрегаты `attendees_*`.
- RSVP write-back (CalDAV)**: изменение статуса “Принято/Отклонено/Возможно/Нет ответа” из контекстного меню повестки с записью обратно в календарь (через CalDAV PUT + refresh).
- **Открытие файлов без дублей**: при клике на встречу/протокол/лог — если файл уже открыт, фокусируем существующую вкладку.
- **CalDAV (Google OAuth) как источник встреч**:
  - discovery аккаунтов и календарей через `tsdav`,
  - устранён критичный нюанс Google: `serverUrl` должен быть корнем `https://apidata.googleusercontent.com/caldav/v2/` (без email), иначе падение discovery (`cannot find homeUrl`) и “пустые” календари,
  - UX в настройках: найденные календари сортируются (основной календарь отмечается `✅ Основной`), “праздники” помечены как необязательные,

### 4.1) Инженерные улучшения/рефакторинг (последний цикл)

- **Offline-first для повестки**: при проблемах сети/CalDAV UI продолжает показывать последние успешные события и явно сообщает, что данные stale (баннер + лог).
- **Оптимизация sync в vault**: индекс `sid -> file` убрал повторные `vault.getFiles()` на каждый event.
- **Дедуп сетевого транспорта**: общий `requestUrlFetch` для `fetch`/`cross-fetch` маршрутизации.
- **Единые константы горизонтов**: 60 дней / 48 часов вынесены в `src/calendar/constants.ts`.
- **Улучшение поддерживаемости settings UI**: `settingsTab.ts` разнесён на секции (без смены поведения), readiness CalDAV вынесен в `src/caldav/`.
- Безопасность логов**: добавлена маскировка токенов/секретов в URL/строках (`src/log/redact.ts`) и юнит‑тесты.

### 5. Документация

- Есть `TZ.md` с детальной проработкой и актуальными уточнениями.
- Есть `README.md` с установкой/сборкой/установкой в Obsidian.

---

## ⚠️ Найденные проблемы

- **Риск секретов в git**: приватные ICS URL с токеном хранятся в настройках плагина Obsidian (обычно это `vault/.obsidian/plugins/assistant/data.json`). При git‑синхронизации vault токены могут быть закоммичены.
- **ICS‑парсер MVP‑уровня**: добавлена базовая развёртка `RRULE` (DAILY/WEEKLY) + `EXDATE`, но всё ещё нет `TZID`, `RECURRENCE-ID`, сложных `BY*` правил и исключений Google (для некоторых календарей может быть критично).
- **Сущности “Встречи/Люди/Проекты”**:
  - “Встречи” — базовый поток есть,
  - “Люди/Проекты”** — есть карточки + команды создания; связи из встреч/протоколов пока в MVP (нужны авто‑ссылки/индексация).
- **Уведомления без fallback**: это правильно для предсказуемости, но требует явных зависимостей (`libnotify-bin`/`yad`) и UX‑диагностики (кнопка “Проверить зависимости”).
- **Coverage ограничен “осмысленным include”**: пороги считаются для набора модулей, которые реально можно покрывать unit‑тестами без Obsidian UI (например `src/calendar/ics.ts`, `src/notifications/*`, `src/caldav/*`, `src/log/*`, `src/vault/*`, `src/ids/*`, `src/calendar/store/*`). Это честно для P2, но важно постепенно расширять include вместе с тестами.
- Использование `any`**: встречается точечно (electron fallback, Obsidian API стыки). Это ожидаемо, но стоит держать под контролем, чтобы `any` не “расползался” по доменной логике.
- Нет автоматизированного CI**: сейчас проверки (test/coverage/typecheck/format) запускаются вручную; риск регрессий при росте кода.
- **Терминология без обратной совместимости**: проект перешёл на модель **Встреча (Event) / Протокол (Protocol)** без миграции старых папок/полей. Это упрощает код, но при обновлении существующего vault может потребоваться ручное переименование папок/заметок.
- **CalDAV “пустой календарь без ошибки”**: для неверного `calendarUrl` Google может возвращать 0 объектов без явного исключения. Добавлена диагностика `PROPFIND` и явная ошибка с советом переподключить календарь через discovery.

---

## 💡 Рекомендации по улучшению (приоритет)

- **P0 (критично)**: защита приватных ICS токенов при git‑sync vault
  - добавить в документацию правило: не коммитить `/.obsidian/plugins/assistant/data.json`
  - (лучше) сделать поддержку “секретов” вне vault (env/локальный файл вне git) и ссылаться на него из настроек
- P0 (критично)**: не допускать утечек секретов в логах/файлах
  - использовать `redactUrlForLog`/`redactSecretsInStringForLog` во всех местах, где логируются ошибки сетевых запросов/URL/параметры OAuth;
  - добавить “проверочный” юнит‑тест на регрессию (пример: ошибка с URL, содержащим `access_token`).
- P1**: Obsidian-native индекс сущностей (md + frontmatter)
  - `.base` файлы для “Bases” уже создаются автоматически (минимальные table views + `file.inFolder("...")`).
  - следующий шаг — добавить авто‑ссылки и поля связей в карточках (например `people: [[...]]`, `projects: [[...]]`) и синхронизировать их из событий/протоколов.
- **P1**: завершить цепочку данных **Встреча → Протоколы → Люди/Проекты**
  - извлечение участников и создание карточек людей уже есть; дальше — связывание встреч/протоколов с карточками людей/проектов через ссылки в frontmatter.
- **P1**: сделать корректный импорт календарей Google (RRULE/timezones)
  - либо полноценный ICS парсер (с RRULE), либо переход на Google Calendar API (если понадобится редактирование описания)
- **P1**: протоколы “current/last” должны быть устойчивы к отсутствию frontmatter cache
  - сейчас “текущий/последний” читается из frontmatter `start` у протокола через `metadataCache`; при первом импорте/без открытия файла кэш может быть пустым — стоит добавить fallback (прочитать файл/распарсить YAML) или индекс
- **P1**: CalDAV: кэшировать `calendarUrl` только через discovery и иметь кнопку “пере‑найти” для календаря
  - чтобы не зависать на “0 событий” из‑за опечатки/смены URL на стороне сервера
- P1**: persistent cache событий (на диск) для “повестки без сети” после рестарта Obsidian
  - ✅ реализовано: `CalendarEventCache` пишет/читает снапшот (без `url`) в системной папке плагина; повестка продолжает работать в stale режиме.
- P1**: тесты на `normalizeSettings` (корректная работа “с нуля”)
  - добавить `tests/settingsStore.test.ts`: дефолты, trim `myEmail`, валидные/невалидные структуры.
- **P2**: явная диагностика зависимостей уведомлений (UX) — ✅ сделано
- **P2**: coverage (Vitest reporter) + минимальный порог — ✅ сделано
  - следующий шаг: постепенно расширять `coverage.include` на новые модули вместе с тестами
- P2**: включить лёгкий CI (GitHub Actions)
  - `npm ci`, `npm run format:check`, `npm run typecheck`, `npm test`, `npm run test:coverage`.
- **P3**: добавить guard на “сборка перед релизом”
  - теперь артефакты сборки живут в `dist/`; полезно иметь CI/проверку, что `npm run build` выполнен и `dist/main.js` соответствует `main.ts`

---

## 📋 Соответствие техническому заданию (`TZ.md`)

### ✅ Полностью реализовано

- Подключение календарей через **ICS URL**
- Панель **“Повестка”** (сетка по часам, пересечения, текущая линия времени)
- Уведомления **за 5 минут** (настраивается) и **в момент начала**
- Панель **“Лог”** + логи в `.log` файлах (в `.obsidian/plugins/assistant/logs/`)
- Встречи календаря как **md‑файлы** + открытие по клику
- **Связь “Встреча ↔ Протоколы”** (создание протокола из встречи, список протоколов во встрече, ссылка на встречу в протоколе)

### 📝 Частично реализовано

- **Сущность “Встречи”** (появился базовый поток, запись/транскрипция пока не реализованы)
- “Люди/Проекты”** (карточки + команды есть; связи/авто‑ссылки и представления “люди из встреч” — следующий шаг)
- “Обновить встречу календаря саммари” (в ТЗ есть, реализации нет)

---

## 🎯 Заключение

### Общая оценка: **4/5** ⭐⭐⭐⭐☆

Хорошая база для дальнейшего развития: архитектура уже позволяет наращивать функциональность без переписывания ядра. Следующий логичный шаг — закрыть безопасность секретов и достроить “прикладной” слой встреч/связей.

**Дата ревью:** 2026-01-20 (обновлено)  
**Ревьюер:** AI Assistant  
**Версия:** 0.0.1
