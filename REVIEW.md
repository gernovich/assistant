# Ревью Assistant (Obsidian plugin)

## Общая оценка: ⭐⭐⭐⭐☆ (4/5)

Проект выглядит **зрелым для MVP календаря/встреч**: есть единый поток данных (ICS/CalDAV → in-memory store → повестка/уведомления → синк в vault), DI/контракты, большой набор юнит‑тестов и CI‑проверки. Ключевые риски на следующий этап — **секреты**, **границы “master event vs occurrence”**, и несколько “дорогих” мест по веточному покрытию/логике.

---

## 📊 Статистика проекта (актуально)

- **Язык/стек**: TypeScript, Obsidian plugin (Electron renderer), esbuild, Vitest
- **Исходный код**: 177 TS файлов в `src/` (~16493 строк)
- **Тесты**: 103 файла `*.test.ts` в `tests/` (~8397 строк); всего `tests/*.ts` = 104 файла
- **Покрытие тестами**: 425 тестов (Vitest), ~1016 `expect(...)` (приблизительная оценка assertions)
- **Комментарии (TSDoc/JSDoc)**: ~459 блоков `/** ... */` в `src/` (приблизительно)
- **Статус тестов**: ✅ `npm test` — все тесты проходят
- **Coverage**: ✅ `npm run test:coverage` (v8): **99.28% statements/lines, 96.45% branches**
- **Скрипты качества**:
  - ✅ `npm run lint` (ESLint, `.eslintrc.cjs`)
  - ✅ `npm run typecheck` (tsc --noEmit)
  - ✅ `npm run format:check` (Prettier)
- **CI**: ✅ GitHub Actions (`.github/workflows/ci.yml`: format → lint → typecheck → test → coverage)
- **Версия**: `0.0.1` (из `package.json`)

---

## ✅ Сильные стороны

### 1. Архитектура и структура

- **Композиция/DI выделены**: `src/plugin/pluginContext.ts` как composition root + Tsyringe контейнер (`src/plugin/di/assistantContainer.ts`). Это заметно снижает связность `main.ts` и упрощает тестирование.
- **Ясное разделение слоёв**:
  - `src/application/*` — use-case’ы,
  - `src/domain/policies/*` — pure policies,
  - `src/*` (calendar/recording/vault/notifications) — инфраструктура и адаптеры.
- **Presentation отдельным контуром**: регистрация views/commands вынесена в контроллер (`src/plugin/assistantController.ts`, `src/presentation/obsidian/*`).

### 2. Качество кода

- **Strict TS** + явные DTO/типы (`src/types.ts`), настройка линтера/форматера, CI.
- **Явные контракты данных** в документации: `TZ.md` (ТЗ) и `FIELDS.md` (frontmatter/DTO контракты).
- **Безопасность логов**: есть отдельная инфраструктура redaction (`src/log/redact.ts`) и тесты.

### 3. Покрытие тестами

- **Высокое покрытие с ветками** для большинства критичных модулей (особенно календарь/ICS и persistent cache).
- **[НОВОЕ]** Критичные “узлы календаря” доведены до 100% branches:
  - `src/calendar/ics.ts`
  - `src/calendar/store/calendarEventCache.ts`

### 4. Реализованный функционал (MVP)

- **Календарь**: refresh нескольких календарей, offline‑first семантика “lastGood/stale” (`src/calendar/calendarService.ts`, `src/calendar/store/calendarEventStore.ts`).
- **Persistent cache** после рестарта без сети (без утечки `url`): `src/calendar/store/calendarEventCache.ts`.
- **ICS парсер (MVP+)**: `RRULE` (DAILY/WEEKLY), `EXDATE`, `RECURRENCE-ID`‑дедуп occurrences, `TZID`, `VALARM`, `ORGANIZER`, `ATTENDEE`, `COLOR` (`src/calendar/ics.ts`).
- **Уведомления**: планировщик таймерами + `electron_window` как основной канал, safety fallback на `Notice` (`src/notifications/notificationScheduler.ts`, `src/notifications/electronWindowReminder.ts`).
- **Запись**: backends (electron media devices / linux ffmpeg), чанки, виз‑метрики, прикрепление файлов к протоколу (`src/recording/backends/useCaseBackends.ts`, `src/recording/backends/linuxFfmpegBackend.ts`, `src/recording/protocolAttachmentService.ts`).
- **Vault сущности**: встречи/протоколы/люди/проекты, `.base` файлы (см. `README.md`, и note‑services в `src/*NoteService.ts`).

### 5. Документация

- `README.md` — установка, build/dev, настройка ICS/CalDAV, команды.
- `TZ.md` — архитектурное ТЗ (сильно помогает держать фокус).
- `FIELDS.md` — контракт полей/идентичностей, включая **EventKey/OccurrenceKey**.
- `TESTING.md` — ручные кейсы и remote debugging.

---

## ⚠️ Найденные проблемы

- **P0 — секреты в git**: приватные ICS URL и CalDAV данные живут в `.obsidian/plugins/assistant/data.json` в vault. При неправильной git‑схеме это легко утечёт.
- **ICS/RRULE ограничен**: парсер целенаправленно не поддерживает сложные RRULE (сложные `BY*` правила), а timezone‑математика для локальных DTSTART (без `Z`) остаётся эвристикой.
- **Event vs Occurrence**:
  - Контракт уже зафиксирован в `FIELDS.md` (**EventKey** vs **OccurrenceKey**), но это важно удержать как “источник истины” и не допустить смешения ключей в UI/use‑case’ах/синке.
- **metadataCache как риск после рестарта**: по `TZ.md` критичные потоки должны иметь fallback и не полагаться на прогретый `metadataCache` для принятия решений.
- **Покрытие веток в нескольких “дорогих” местах**:
  - `src/calendar/store/calendarEventStore.ts` (branches ~86%)
  - `src/log/logService.ts` (branches ~83%)
  - `src/vault/entityIndex.ts` (branches ~94%)

---

## 💡 Рекомендации по улучшению (приоритет)

- **P0 (критично) — секреты**:
  - зафиксировать “железный” workflow для git‑vault: `.obsidian` не коммитим; добавить явный блок в `README.md`/`TESTING.md`,
  - дополнительно: рассмотреть вынос секретов из vault (внешний локальный файл/OS keychain/ENV) и ссылку из настроек.
- **P1 — довести Event vs Occurrence до полного “контракта в коде”**:
  - use-case’ы/IPC должны чётко различать операции “про master” и “про конкретную дату” (OccurrenceKey),
  - для write‑back CalDAV зафиксировать правила: master vs override по `RECURRENCE-ID`.
- **P1 — убрать зависимость от `metadataCache` для логики**:
  - fallback: чтение файла + парсинг frontmatter (policies) или индекс/кэш.
- **P2 — добить оставшиеся coverage hotspots**:
  - `CalendarEventStore` (ветки stale/disabled/index rebuild),
  - `LogService` (ветки ограничения/санитизация),
  - `EntityIndex` (ветки редких фронтматтер кейсов).
- **P3 — вернуть/пересобрать документ рефакторинга**:
  - если `REFACTORIN.md` нужен как “контрольный список”, лучше восстановить его в репозитории (или заменить на более короткий `ARCHITECTURE.md` + `ROADMAP.md`).

---

## 📋 Соответствие техническому заданию (`TZ.md`)

### ✅ Полностью реализовано (ядро MVP)

- Подключение календарей: **ICS URL**, **CalDAV (включая Google OAuth)**
- Повестка (agenda view) + навигация
- Уведомления “за N минут” и “в момент начала” (electron window, fallback на Notice)
- Создание/обновление карточек встреч и протоколов в vault + базовые связи
- Offline‑first поведение (lastGood/stale) + persistent cache на диск

### 📝 Частично реализовано / следующий этап

- Запись/диктофон есть, но транскрипция/извлечение знаний (ASR/LLM pipeline) — ещё вне текущего MVP
- “Люди/Проекты” как сущности есть, но связи/индексация и UX уровня “досье/граф” — следующий слой
- Полноценный RRULE/timezone‑движок для сложных календарей

---

## 🎯 Заключение

### Общая оценка: **4/5** ⭐⭐⭐⭐☆

**[НОВОЕ]** Проект готов к следующему этапу: базовый календарный контур и инфраструктура качества (tests/coverage/CI) уже на уровне, который позволяет безопасно добавлять продуктовую логику. Самые важные “тормоза” для роста — безопасность секретов и строгая дисциплина EventKey/OccurrenceKey в доменных сценариях.

### Рекомендации

- В первую очередь закрыть **секреты** и закрепить **Event vs Occurrence** как “контракт в коде”.
- Затем добить оставшиеся веточные hotspots (это дешёвый способ снизить риск регрессий).

### Вердикт

Можно двигаться дальше, не возвращаясь к “переписыванию ядра”: архитектура и тестовая база уже поддерживают итерации.

---

**Дата ревью:** 2026-01-23  
**Ревьюер:** AI Assistant  
**Версия:** 0.0.1  
