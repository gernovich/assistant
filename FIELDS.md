# FIELDS — поля сущностей и реализация в проекте

Документ описывает **термины** и **поля событий календаря** и фиксирует:
- где поле **берётся** (ICS/CalDAV),
- где поле **хранится** (наши типы/кэш),
- где поле **используется** (повестка/карточка/уведомления),
- что пока **не поддержано** (MVP-ограничения).

> Важно: часть полей относится к “событию в целом” (master), а часть — к **конкретной дате/экземпляру** (occurrence). 
> В CalDAV/Google это часто выражается отдельными VEVENT (например override с `RECURRENCE-ID`).

# DTO

| Имя       | Класс        | Назначение                                                                                             |
|-----------|--------------|--------------------------------------------------------------------------------------------------------|
| Календарь | `Calendar`   | Runtime‑сущность подключённого календаря (id/name/type + config) для refresh/статусов                  |
| Встреча   | `Event`      | Runtime‑событие календаря для повестки/уведомлений/синка в карточку встречи                            |
| Событие   | `Occurrence` | Экземпляр (occurrence) повторяющейся встречи для работы с конкретной датой (протокол/файлы/обновление) |
| Протокол  | `Protocol`   | Vault‑сущность протокола встречи (transcript/summary/files + связи с людьми/проектами)                 |
| Человек   | `Person`     | Vault/runtime‑сущность человека (контакты/атрибуты) для связей и отображения                           |
| Проект    | `Project`    | Vault/runtime‑сущность проекта для связей с протоколами/людьми и статусов                              |



## Vault/frontmatter схемы (контракт)

### Ключи идентичности (общие правила)

В проекте есть две разные “идентичности”, и их нельзя смешивать:

- **EventKey (master встречи)**: `event_key = calendar_id:event_id`
  - Используется для индексов, карточки встречи (один файл на series/master) и общих операций “про встречу в целом”.
- **OccurrenceKey (экземпляр/дата встречи)**: `occurrence_key = calendar_id:event_id:start_ms`
  - `start_ms` = `Date(start).getTime()` (epoch ms).
  - Используется в IPC/UI/use-cases, когда надо однозначно выбрать **конкретную дату** (запись/протокол/точечный write-back).
  - Для не-recurring встреч `OccurrenceKey` всё равно определён и равен master+дата.

### Карточка встречи (`assistant_type: calendar_event`)

**Обязательные**:
- `assistant_type: calendar_event`
- `calendar_id: string`
- `event_id: string`
- `summary: string`
- `start: ISO string`

**Опциональные**:
- `end?: ISO string`
- `url?: string`
- `location?: string`
- `status?: accepted|declined|tentative|needs_action|unknown`
- `organizer_email?: string`
- `organizer_cn?: string`
- `attendees?: string[]` (`person_id`)
- `attendees_accepted?: string[]` (`person_id`)
- `attendees_declined?: string[]` (`person_id`)
- `attendees_tentative?: string[]` (`person_id`)
- `attendees_needs_action?: string[]` (`person_id`)
- `attendees_unknown?: string[]` (`person_id`)
- `timezone?: string`
- `rrule?: string`
- `reminders_minutes_before?: number[]`
- `event_color?: string`

**Стабильный ключ (EventKey, для индексов, не поле)**: `calendar_id:event_id`

### Карточка протокола (`assistant_type: protocol`)

**Обязательные**:
- `assistant_type: protocol`
- `protocol_id: string` (формат: `calendar_id:event_id`)
- `calendar_id: string`
- `start: ISO string`
- `occurrence_key: string` (формат: `calendar_id:event_id:start_ms`) — **ключ экземпляра**, должен однозначно указывать “какая дата” протокола
  - Для обратной совместимости старых протоколов допускается отсутствие `occurrence_key`, тогда экземпляр определяется по `(calendar_id, protocol_id, start)`.

**Опциональные**:
- `end?: ISO string`
- `summary?: string`
- `transcript?: string`
- `files?: string[]` — список путей на аудио‑файлы в vault (обычно `Ассистент/Записи/...`)
- `recurrence_id?: ISO string` — CalDAV `RECURRENCE-ID` (если протокол привязан к override-экземпляру)
- `participants?: Array<{ person_id?: string; display_name?: string; email?: string }>`
- `projects?: Array<{ project_id?: string; title?: string }>`

### Карточка человека (`assistant_type: person`)

**Обязательные**:
- `assistant_type: person`
- `person_id: string`

**Опциональные**:
- `display_name?: string`
- `first_name?: string`
- `last_name?: string`
- `middle_name?: string`
- `nick_name?: string`
- `gender?: string`
- `photo?: string`
- `birthday?: string`
- `voiceprint?: string`
- `emails?: string[]`
- `phones?: Array<{ label?: string; value: string }>`
- `companies?: string[]`
- `positions?: string[]`
- `mailboxes?: string[]`
- `messengers?: Array<{ kind: string; handle: string; url?: string }>`

### Карточка проекта (`assistant_type: project`)

**Обязательные**:
- `assistant_type: project`
- `project_id: string`
- `title: string`

**Опциональные**:
- `status?: string`
- `owner?: { person_id?: string; display_name?: string; email?: string }`
- `tags?: string[]`
- `protocols?: Array<{ protocol_id: string; start?: string; end?: string; summary?: string }>`

# Наша модель данных (источник истины для UI)

## Календарь Calendar

**DTO календаря (runtime)**: `Calendar`
**DTO конфига календаря (persisted)**: `CalendarConfig`
**DTO аккаунта (runtime)**: `CalendarAccount`
**DTO конфига аккаунта (persisted)**: `CalendarAccountConfig` (= `CaldavAccountConfig`)

Статус: ✅ (DTO есть, настройки/рендер в UI реализованы)

| Имя           | В коде            | В БД (настройки) | В представлении | Синхронизация            |
|---------------|-------------------|------------------|-----------------|--------------------------|
| Код календаря | `Calendar.id`     | `settings.json`  |                 | Пользователь → настройки |
| Имя           | `Calendar.name`   | `settings.json`  | + (в tooltip)   | Пользователь → настройки |
| Тип           | `Calendar.type`   | `settings.json`  |                 | Пользователь → настройки |
| Конфиг        | `Calendar.config` | `settings.json`  |                 | Пользователь → настройки |

## Конфиг календаря CalendarConfig

| Имя            | В коде                              | В БД (настройки) | В представлении | Синхронизация            |
|----------------|-------------------------------------|------------------|-----------------|--------------------------|
| Код календаря  | `CalendarConfig.id`                 | `settings.json`  |                 | Пользователь → настройки |
| Имя            | `CalendarConfig.name`               | `settings.json`  |                 | Пользователь → настройки |
| Тип            | `CalendarConfig.type`               | `settings.json`  |                 | Пользователь → настройки |
| Включён        | `CalendarConfig.enabled`            | `settings.json`  |                 | Пользователь → настройки |
| ICS URL        | `CalendarConfig.url?`               | `settings.json`  |                 | Пользователь → настройки |
| CalDAV account | `CalendarConfig.caldav.accountId`   | `settings.json`  |                 | Пользователь → настройки |
| CalDAV url     | `CalendarConfig.caldav.calendarUrl` | `settings.json`  |                 | Пользователь → настройки |

## Аккаунт CalendarAccount

| Имя          | В коде                        | В БД (настройки) | В представлении | Синхронизация            |
|--------------|-------------------------------|------------------|-----------------|--------------------------|
| Код аккаунта | `CalendarAccount.id`          | `settings.json`  |                 | Пользователь → настройки |
| Имя          | `CalendarAccount.name`        | `settings.json`  |                 | Пользователь → настройки |
| Включён      | `CalendarAccount.enabled`     | `settings.json`  |                 | Пользователь → настройки |
| Сервер       | `CalendarAccount.serverUrl`   | `settings.json`  |                 | Пользователь → настройки |
| Логин        | `CalendarAccount.username`    | `settings.json`  |                 | Пользователь → настройки |
| Auth метод   | `CalendarAccount.authMethod?` | `settings.json`  |                 | Пользователь → настройки |

## Конфиг аккаунта CalendarAccountConfig

| Имя          | В коде                            | В БД (настройки) | В представлении | Синхронизация            |
|--------------|-----------------------------------|------------------|-----------------|--------------------------|
| Код аккаунта | `CalendarAccountConfig.id`        | `settings.json`  |                 | Пользователь → настройки |
| Имя          | `CalendarAccountConfig.name`      | `settings.json`  |                 | Пользователь → настройки |
| Включён      | `CalendarAccountConfig.enabled`   | `settings.json`  |                 | Пользователь → настройки |
| Сервер       | `CalendarAccountConfig.serverUrl` | `settings.json`  |                 | Пользователь → настройки |
| Логин        | `CalendarAccountConfig.username`  | `settings.json`  |                 | Пользователь → настройки |
| Пароль       | `CalendarAccountConfig.password`  | `settings.json`  |                 | Пользователь → настройки |
| OAuth        | `CalendarAccountConfig.oauth?`    | `settings.json`  |                 | Пользователь → настройки |

## Встреча Event “событию в целом” (master)

Важный момент, Встреча обновляется под атуальное Событие, а Событие может быть и в прошлом и в будущем и текущще.

Статус: ✅ (DTO/парсинг/карточка/кэш есть) + 🚧 (цвет события CalDAV — цель)

| Имя           | В коде              | CalDAV                               | ICS                                  | В карточке встречи (frontmatter)  | В представлении | Синхронизация                                       |
|---------------|---------------------|--------------------------------------|--------------------------------------|-----------------------------------|-----------------|-----------------------------------------------------|
| Код           | `Event.id`          | `UID`                                | `UID`                                | `event_id`                        |                 | Календарь → плагин → vault                          |
| Код календаря | `Event.calendar.id` | (внутр. ID в настройках плагина)     | (внутр. ID в настройках плагина)     | `calendar_id`                     |                 | Настройки → плагин → vault                          |
| Заголовок     | `Event.summary`     | `SUMMARY`                            | `SUMMARY`                            | `summary`                         | +               | Календарь → плагин → vault                          |
| Описание      | `Event.description` | `DESCRIPTION`                        | `DESCRIPTION`                        | (тело карточки)                   |                 | Календарь → плагин → vault                          |
| Место         | `Event.location`    | `LOCATION`                           | `LOCATION`                           | `location`                        |                 | Календарь → плагин → vault                          |
| Ссылка        | `Event.url`         | `URL`                                | `URL`                                | `url`                             |                 | Календарь → плагин → vault                          |
| Начало        | `Event.start`       | `DTSTART`                            | `DTSTART`                            | `start`                           | +               | Календарь → плагин → vault                          |
| Конец         | `Event.end`         | `DTEND`                              | `DTEND`                              | `end`                             | +               | Календарь → плагин → vault                          |
| Весь день     | `Event.allDay`      | `DTSTART` (VALUE=DATE)               | `DTSTART` (VALUE=DATE)               |                                   |                 | Календарь → плагин → vault                          |
| Часовой пояс  | `Event.timeZone`    | `TZID` / `VTIMEZONE`                 | `TZID` / `VTIMEZONE`                 | `timezone`                        |                 | Календарь → плагин → vault (TZID/UTC как строка)    |
| Организатор   | `Event.organizer`   | `ORGANIZER`                          | `ORGANIZER`                          | `organizer_email`, `organizer_cn` | +               | Календарь → плагин → vault                          |
| Гости         | `Event.attendees`   | `ATTENDEE`                           | `ATTENDEE`                           | `attendees` + `attendees_*`       | + (агрегаты)    | Календарь → плагин → vault                          |
| Статус        | `Event.status`      | `PARTSTAT` (RSVP)                    | `PARTSTAT` (read-only)               | `status`                          | +               | CalDAV: календарь ↔ плагин → vault                  |
| Повторения    | `Event.recurrence`  | `RRULE` / `EXDATE` / `RECURRENCE-ID` | `RRULE` / `EXDATE` / `RECURRENCE-ID` | `rrule`                           |                 | Календарь → плагин → vault (сейчас сохраняем RRULE) |
| Уведомления   | `Event.reminders`   | `VALARM`                             | `VALARM`                             | `reminders_minutes_before`        |                 | Календарь → плагин → vault (сейчас minutesBefore)   |
| Цвет          | `Event.color`       | event color (цель)                   | `COLOR`                              | `event_color`                     |                 | Сейчас: ICS → плагин → vault (COLOR); CalDAV: цель  |

**Идентичность встречи (в vault)**: `(calendar_id, event_id)` (одно `event_id` без `calendar_id` не уникально).  
**Стабильный ключ для индексов**: `calendar_id:event_id` (вычисляется, отдельным полем не хранится).


## Событие Occurrence (конкретная дата/экземпляр повторяющейся встречи)

Статус: ✅ (контракт оформлен; в коде occurrences пока часто представлены как `Event` со `start` и `recurrence.recurrenceId?`)

Occurrence нужен, чтобы обновлять **конкретный экземпляр** повторяющейся встречи (а не master-событие).
Пример: встреча уже прошла → сделали протокол, прикрепили файлы, расшифровали, сделали summary → и хотим обновить описание/статус **именно этой даты** в календаре (CalDAV).

Минимальные поля для Occurrence (чтобы можно было “точечно обновлять” в CalDAV):

| Поле            | Тип          | Обязательность | Источник                 | Зачем                                                         |
|-----------------|--------------|----------------|--------------------------|---------------------------------------------------------------|
| `calendar_id`   | `string`     | обяз.          | настройки плагина        | однозначная идентичность вместе с `event_id`                  |
| `event_id`      | `string`     | обяз.          | CalDAV/ICS (`UID`)       | однозначная идентичность вместе с `calendar_id`               |
| `start`         | `ISO string` | обяз.          | CalDAV/ICS (`DTSTART`)   | выбрать/обновить конкретную дату экземпляра                   |
| `occurrence_key`| `string`     | обяз.          | вычисляется              | единый ключ экземпляра: `calendar_id:event_id:start_ms`       |
| `recurrence_id` | `ISO string` | опц.           | CalDAV (`RECURRENCE-ID`) | точечное обновление override‑экземпляра (если провайдер даёт) |
| `sequence`      | `number`     | опц.           | CalDAV (`SEQUENCE`)      | контроль конфликтов/версий при записи обратно                 |
| `last_modified` | `ISO string` | опц.           | CalDAV (`LAST-MODIFIED`) | контроль конфликтов/версий при записи обратно                 |

## Протокол Protocol

Статус: ✅ (DTO + шаблон карточки + `.base` колонки есть)

| Имя                         | В коде                     | В карточке (frontmatter)                                | В представлении | Синхронизация            |
|-----------------------------|----------------------------|---------------------------------------------------------|-----------------|--------------------------|
| Код                         | `Protocol.id`              | `protocol_id`                                           | +               | Встреча → плагин → vault |
| Код календаря               | `ProtocolNote.calendar_id` | `calendar_id`                                           |                 | Встреча → плагин → vault |
| Начало                      | `Protocol.start`           | `start`                                                 | +               | Встреча → плагин → vault |
| Конец                       | `Protocol.end?`            | `end`                                                   | +               | Встреча → плагин → vault |
| Краткое содержание          | `Protocol.summary?`        | `summary`                                               |                 | Пользователь → vault     |
| Расшифровка (транскрибация) | `Protocol.transcript?`     | `transcript`                                            |                 | Пользователь → vault     |
| Файлы (массив)              | `Protocol.files?[]`        | `files`                                                 |                 | Пользователь → vault / запись → vault |
| Участники (массив)          | `Protocol.participants?[]` | `participants`                                          |                 | Пользователь → vault     |
|                             | (вложенный тип)            | `Array<PersonLinkDto>` (`person_id/display_name/email`) |                 |                          |
| Проекты (массив)            | `Protocol.projects?[]`     | `projects`                                              |                 | Пользователь → vault     |
|                             | (вложенный тип)            | `Array<ProjectLinkDto>` (`project_id/title`)            |                 |                          |

**Формат `protocol_id`**: `calendar_id:event_id` (это ID встречи, к которой относится протокол).

В теле заметки (markdown):
- ✅ **Расшифровка** (основной текст транскрипта)

### Примечание по полю `files`

- **Как хранится**: в frontmatter это “плоский” ключ, поэтому массив хранится в JSON‑виде (пример: `files: ["Ассистент/Записи/a.webm", "Ассистент/Записи/b.webm"]`).
- **Как заполняется**:
  - вручную (пользователь)
  - автоматически: при записи с включённым “Новый протокол” диктофон добавляет каждый созданный аудио‑файл в `files`.

## Человек Person

Статус: ✅ (DTO + шаблон карточки + `.base` колонки есть)

| Имя                     | В коде                 | В карточке (frontmatter) | В представлении | Синхронизация        |
|-------------------------|------------------------|--------------------------|-----------------|----------------------|
| Код                     | `Person.id`            | `person_id`              |                 | Пользователь → vault |
| Отображать              | `Person.displayName?`  | `display_name`           | +               | Пользователь → vault |
| Имя                     | `Person.firstName?`    | `first_name`             |                 | Пользователь → vault |
| Фамилия                 | `Person.lastName?`     | `last_name`              |                 | Пользователь → vault |
| Отчество                | `Person.middleName?`   | `middle_name`            |                 | Пользователь → vault |
| Кличка                  | `Person.nickName?`     | `nick_name`              |                 | Пользователь → vault |
| Пол                     | `Person.gender?`       | `gender`                 |                 | Пользователь → vault |
| Фото                    | `Person.photo?`        | `photo`                  |                 | Пользователь → vault |
| ДР                      | `Person.birthday?`     | `birthday`               |                 | Пользователь → vault |
| Emails (массив)         | `Person.emails?[]`     | `emails`                 | +               | Пользователь → vault |
| Телефоны (массив)       | `Person.phones?[]`     | `phones`                 | +               | Пользователь → vault |
| Компании (массив)       | `Person.companies?[]`  | `companies`              |                 | Пользователь → vault |
| Должности (массив)      | `Person.positions?[]`  | `positions`              |                 | Пользователь → vault |
| Voiceprint              | `Person.voiceprint?`   | `voiceprint`             |                 | Пользователь → vault |
| Почтовые ящики (массив) | `Person.mailboxes?[]`  | `mailboxes`              |                 | Пользователь → vault |
| Мессенджеры (массив)    | `Person.messengers?[]` | `messengers`             |                 | Пользователь → vault |

В теле заметки (markdown):
- ✅ **Досье** (ручное описание человека)
- ✅ **Факты** (собранные факты/наблюдения)

## Проект Project

Статус: ✅ (DTO + шаблон карточки + `.base` колонки есть)

| Имя                | В коде                 | В карточке (frontmatter)                                        | В представлении | Синхронизация        |
|--------------------|------------------------|-----------------------------------------------------------------|-----------------|----------------------|
| Код                | `Project.id`           | `project_id`                                                    |                 | Пользователь → vault |
| Название           | `Project.title`        | `title`                                                         | +               | Пользователь → vault |
| Статус             | `Project.status?`      | `status`                                                        |                 | Пользователь → vault |
| Владелец           | `Project.owner?`       | `owner`                                                         |                 | Пользователь → vault |
|                    | (вложенный тип)        | `{ person_id?: string; display_name?: string; email?: string }` |                 |                      |
| Теги               | `Project.tags?[]`      | `tags`                                                          |                 | Пользователь → vault |
| Протоколы (массив) | `Project.protocols?[]` | `protocols`                                                     |                 | Пользователь → vault |

В теле заметки (markdown):
- ✅ **Заметки**
- ✅ **Обещания**
- ✅ **Статусы**

## Цвет EventColor

| Имя  | В коде              | В БД | В представлении | Синхронизация |
|------|---------------------|------|-----------------|---------------|
| ID   | `EventColor.id?`    |      |                 |               |
| Имя  | `EventColor.name?`  |      |                 |               |
| Цвет | `EventColor.value?` |      |                 |               |

Сейчас используем только **цвет события из ICS `COLOR`**:
- `Event.color.value` → frontmatter встречи `event_color`

